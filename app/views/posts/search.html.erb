<h3>Search Result</h3>  

    <% @results.each do |result| %>  
        <%= result.title %><br>  
        <%= result.body %><br>
        <%= image_tag(result.photo, width: 300) if result.photo.attached? %>
    <% end %>